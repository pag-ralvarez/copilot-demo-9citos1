import { Routes } from '@angular/router';
import { ShoppingComponent } from './components/shopping/shopping.component';

export const routes: Routes = [
  // add route for shopping component
  { path: '', component: ShoppingComponent },
];
